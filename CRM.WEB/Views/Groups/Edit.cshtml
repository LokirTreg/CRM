@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model CRM.WEB.Models.Entyties.Group
<h1>Изменить даннный Группы</h1>
@if (Model is null)
{
    <p>Нет такой группы</p>
}
else
{
    <form method="post">
        <div class="mt-3">
            <label class="form-label">Id</label>
            <input type="text" class="form-control" asp-for="Id" readonly />
        </div>
        <div class="mt-3">
            <label class="form-label">Номер</label>
            <input type="number" class="form-control" asp-for="Number" />
        </div>
        <div class="mt-3">
            <button type="submit" class="btn btn-success">Сохранить</button>
        </div>@*
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Номер</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var group in ViewBag.ListStudents)
                {
                    <tr>
                        <td>@group.Text</td>
                    </tr>
                }
            </tbody>
    </table>*@
        <div class="mt-3">
            <button type="submit" class="btn btn-danger" asp-action="Delete" asp-controller="Groups">Удалить</button>
        </div>
    </form>
}