@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<CRM.WEB.Models.StudentDetailView>
<h1>Студенты</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Фио</th>
            <th>Почта</th>
            <th>Номер группы</th>
            <a class="btn btn-success btn-block" asp-controller="Students" asp-action="Add">Добавить</a>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.student.Id</td>
                <td>@student.student.Name</td>
                <td>@student.student.Email</td>
                <td>@student.gro.Number</td>
                <td>
                    <a class="btn btn-primary" asp-controller="Students" asp-action="Edit" asp-route-id="@student.student.Id">
                        Изменить
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>